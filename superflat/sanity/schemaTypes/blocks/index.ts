import {heroImageBlock} from './heroImage'
import {heroCodeSketchBlock} from './heroCodeSketch'
import {pageHeaderBlock} from './pageHeader'
import {titleBlock} from './title'
import {thinkingGalleryBlock} from './thinkingGallery'
import {thinkingBlock} from './thinking'
import {artefactBlock} from './artefact'
import {codeCanvasBlock} from './codeCanvas'
import {textBlock} from './text'
import {imageBlock} from './image'

export const blockTypes = [
  heroImageBlock,
  heroCodeSketchBlock,
  pageHeaderBlock,
  titleBlock,
  thinkingGalleryBlock,
  thinkingBlock,
  artefactBlock,
  codeCanvasBlock,
  textBlock,
  imageBlock,
]
