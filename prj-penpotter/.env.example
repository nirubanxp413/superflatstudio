# In-iframe orchestration (Vercel AI SDK + OpenAI). Do not commit real keys.
VITE_LLM_API_KEY=

# Optional: Node Mastra sidecar — same tools, real @mastra/core. Run: cd server && npm start
# VITE_MASTRA_WS_URL=ws://127.0.0.1:3456
